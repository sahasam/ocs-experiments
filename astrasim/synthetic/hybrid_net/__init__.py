"""Hybrid packet/circuit network model — Python analytical, peer to AstraSim.

Consumes the same Chakra ET that feeds AstraSim, applies a TDM+packet timing
model to chosen network tiers, and emits the same metric panel as
parse_results.py so the two pipelines can be compared side-by-side.

See plan: /Users/sahas/.claude/plans/sure-let-s-go-with-hidden-thimble.md
"""
